import React from 'react';
import Layout from '../components/layout';

const Blog = () => {        
    return (
        <Layout>
            This is the blog template for
        </Layout>
    );
};

export default Blog;